#include <stdio.h>

int main(int argc, char *argv[])
{
    int c = 10;
    while (c > 0) {
        puts("I must not write code in class");
        c = c - 1;
        printf("%d\n", c);
    }
    return 0;
}
