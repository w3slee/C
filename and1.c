/*
 *
 * && (AND) operator -> checks if two conditions are true, evaluates to true only if both
 *       conditions are true.
 * The AND operator is efficient, if the first condition is false the computer wont
 * bother evaluating the second condition because it knows both of them need to be true
 * in order for execution to proceed.
 * */
int main(int argc, char *argv[])
{
    int dealer_up_card = 0;
    int hand = 0;
    int double_down = 0;
    if ((dealer_up_card == 6) && (hand == 11)){
        double_down
    }
        return 0;
}
