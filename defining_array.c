/*
 * This is how you define an array in C
 * 
 * s = {'t', 'a', 'r', 's', ...};
 *
 * Each of the char in the string is just an element in an array
 * That is why we can refer to individual char in the string by using an index
 * like
 *
 * s[0] and s[1]
 *
 * */

